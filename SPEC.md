# 제품 명세 (SPEC)

행사 진행자(관리자)와의 Socratic 인터뷰로 확정된 요구사항과 데이터 모델을 정리합니다.

## 1. 한눈에 보기

해커톤 현장용 웹앱. 관객 투표, 심사위원 평가, 경품 추첨을 한 곳에서 운영한다.
Vercel + Supabase 위에서 동작하며, 참가자는 QR/링크로 접속한다.

## 2. 확정된 의사결정 (인터뷰 결과)

| # | 주제 | 결정 |
| --- | --- | --- |
| 1–4 | 개요·스택·관객 식별·심사 접근 | Next.js+Supabase / 관객은 이름 입력 후 익명 투표 / 심사위원은 **개별 코드** 입장 |
| 5 | 투표 익명성 | **구조적 무기명** — 투표 토큰과 추첨 신원 토큰을 분리, 연결 FK 없음 |
| 6 | 심사 루브릭 | **항목 + 가중치 + 코멘트** — 항목별 최대점/가중치, 심사위원은 항목별 점수와 코멘트 입력, 가중 합산 자동 |
| 7 | 관리자 인증 | **관리자 패스코드**(`ADMIN_PASSCODE`, 서버 검증 + HMAC httpOnly 쿠키) |
| 8 | 추첨 | **앱 내 추첨 버튼**(seed 기반 재현 가능) + CSV 내보내기 |
| 9 | 시상 구조 | **별도 시상** — 인기상 1팀, 심사위원상 대상1·최우수2·우수2 |
| 10 | 심사 결과 공개 | **관리자만** — 관객 리빌엔 인기 투표만 |

## 3. 사용자 플로우

### 관객 (`/vote`)
1. 이름(필수)·소속(선택) 입력 → `raffle_entries` 에 신원 등록(추첨 응모).
2. 공개된 참가팀 목록에서 **최대 N팀**(관리자 설정) 투표. 토글로 선택/해제.
3. 첫 투표 시 추첨 신원의 `participated=true` 로 표시(별도 호출, 투표 토큰과 무관).
4. “투표 완료” → 감사 화면. 결과는 행사 종료 후 공개.

### 심사위원 (`/judge`)
1. 개별 발급 코드 입력 → 검증되면 작업 공간 로드(localStorage에 코드 보관).
2. 팀별로 항목 점수(슬라이더)·항목 코멘트·총평 입력.
3. **임시저장**(submitted=false) / **제출**(submitted=true). 심사 마감 전까지 수정 가능.

### 관리자 (`/admin`)
- **대시보드**: 진행 상태(준비/투표/마감/공개) 전환, 심사 열기/마감, “지금 발표 중” 지정,
  행사명·최대 투표수·관객상 이름 설정, 실시간 투표 현황(관객 비공개).
- **참가팀**: 팀/프로젝트/소개/설명/썸네일/링크/팀원/순서/공개여부 CRUD.
- **심사기준**: 항목명·설명·최대점·가중치·순서 CRUD.
- **심사위원**: 이름 등록 시 코드 자동 발급, 활성/비활성, 코드 재발급, 삭제.
- **결과**: 인기상·심사위원상 시상 요약, 관객 전체 순위, 심사 점수 상세(항목별 평균·종합%).
- **추첨**: 참여자 대상 무작위 추첨(seed 표시), 당첨 초기화, CSV 내보내기.

### 화면 (`/display`) / 리빌 (`/reveal`)
- `display`: 프로젝터용. 큰 참여 QR + 현재 발표 팀. 8초 자동 갱신.
- `reveal`: `revealed` 상태에서만 인기상 + 관객 순위 공개. 그 전엔 대기 화면.

## 4. 데이터 모델

- `settings` (단일 행): `phase`(draft/voting/closed/revealed), `current_participant_id`,
  `max_votes_per_voter`, `judging_open`, `popularity_award_label`, `event_name`.
- `participants`: 팀/프로젝트 메타데이터, `published`, `display_order`.
- `votes`: `participant_id` + `voter_token` (UNIQUE). **익명** — 신원과 연결 안 됨.
- `raffle_entries`: `raffle_token`(UNIQUE) + 이름/소속 + `participated` + `is_winner`.
- `judges`: 이름 + `code`(UNIQUE) + `active`.
- `criteria`: 라벨/설명/`max_score`/`weight`/순서.
- `evaluations`: (judge, participant) UNIQUE + 총평 + `submitted`.
- `evaluation_scores`: (evaluation, criterion) UNIQUE + 점수 + 코멘트.
- 뷰 `vote_counts`: 팀별 득표 집계(서버 service role 전용).

### 점수 계산
- 심사 종합(%) = Σ(항목점수 × 가중치) / Σ(항목최대점 × 가중치) × 100, 제출된 심사만, 심사위원 평균.
- 인기상 = 관객 득표 1위(>0표).
- 심사위원상 = 종합% 내림차순 상위 5 → 대상1·최우수2·우수2.

## 5. 보안 모델

- 브라우저에는 **anon 키만** 노출. RLS로 `settings`·공개 `participants` 외 직접 접근 차단.
- 모든 권한 작업(투표/추첨/심사/관리)은 서버 액션에서 **service role** 로 수행하며,
  관리자 작업은 매 호출마다 패스코드 쿠키를 재검증(기본 차단).
- 익명성은 **구조적** 보장: `vote_token` 과 `raffle_token` 은 서로 다른 테이블의
  독립 토큰이며 연결 키가 없다. 클라이언트도 둘을 함께 전송하지 않는다.

## 6. 기본값/가정 (관리자가 변경 가능)

- 단일 행사(설정 싱글톤). 1인 최대 투표수 기본 3.
- 참가팀/심사기준/심사위원은 관리자에서 자유롭게 편집. 시드에 데모 데이터 포함.
- 위협 모델은 현장 행사 수준: `voter_token` 은 기기 localStorage 기반(브라우저 초기화 시
  재투표 가능)으로, 강한 신원확인보다 마찰 없는 참여를 우선한다.
